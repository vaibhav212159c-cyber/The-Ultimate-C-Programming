#include <stdio.h>

int main ()
{
    int r=6,h=4;
    printf ("the area of circle of r = %d is %f\n",r,3.14*r*r);
    printf ("the volume of clylinder of redius %d and height %d is %f",r,h,3.14*r*r*h);
     

    return 0;
}