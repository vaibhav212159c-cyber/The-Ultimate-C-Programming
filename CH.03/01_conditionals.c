#include <stdio.h>

int main ()
{
    //int age =5; // don't do anything because age = 5 and if statement only run if age is greter then 10 
    int age = 15;// run the printf function 
    if (age>10){
        printf("your age is grether then 10");
    }
    return 0;
}